package com.github.deShortOne.end_to_end_encryption;

public enum MessageType {

	// Hopefully can reduce friendaccept etc. down to just accept or reject
	SEND, FROM, NEWFRIEND, FRIENDACCEPT, FRIENDREJECT, YES, NO;
}
